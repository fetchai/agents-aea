name: echo
authors: Fetch.AI Limited
version: 0.1.0
license: Apache 2.0
url: ""
connections:
  connection:
    name: my-oef-connection
    type: oef
    config:
      addr: 127.0.0.1
      port: 10000
default_connection:
behaviours:
  - class_name: EchoBehaviour
    args:
      foo: bar
handler:
  class_name: EchoHandler
  args:
    foo: bar
    bar: foo
tasks:
  - class_name: EchoTask
    args:
      foo: bar
      bar: foo
protocol: "default"
