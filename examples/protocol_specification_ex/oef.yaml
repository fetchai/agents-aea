name: oef
author: fetchai
version: 0.1.0
license: Apache 2.0
description: 'A protocol for interacting with an OEF service.'
speech_acts:
  register_service:
    service_description: Description
    service_id: str
  register_agent:
    agent_description: Description
    agent_id: str
  unregister_service:
    service_description: Description
    service_id: str
  unregister_agent:
    agent_description: Description
    agent_id: str
  search_services:
    query: Query
  search_agents:
    query: Query
  search_result:
    agents: list
  oef_error:
    operation: OEFErrorOperation
  dialogue_error:
    dialogue_id: int
    origin: str

# search_result -> agents is a tuple of type list. Declare each a in 'agents' is of type str.