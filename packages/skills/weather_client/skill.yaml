name: weather_client
authors: Fetch.AI Limited
version: 0.1.0
license: Apache 2.0
url: ""
description: "The weather client skill looks for weather stations to buy weather data from."
behaviours:
  - behaviour:
      class_name: MyBuyBehaviour
      args:
        foo: bar
handlers:
  - handler:
      class_name: FIPAHandler
      args:
        foo: bar
  - handler:
      class_name: DefaultHandler
      args:
        foo: bar
  - handler:
      class_name: OEFHandler
      args:
        foo: bar
tasks:
  - task:
      class_name: EmptyTask
      args:
        foo: bar
shared_classes: []
protocols: ['fipa','default','oef']
