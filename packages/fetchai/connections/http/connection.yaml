name: http
author: fetchai
version: 0.1.0
license: Apache-2.0
fingerprint: ""
description: "The HTTP connection that wraps a RESTful API specification."
class_name: HTTPConnection
protocols: [http]
restricted_to_protocols: ["fetchai/http:0.1.0"]
excluded_protocols: ["fetchai/http:0.1.0"]
config:
  addr: ${addr:127.0.0.1}
  port: ${port:8000}
  api: '' # put here the dotted path to your API spec name.
dependencies:
  api: {}
