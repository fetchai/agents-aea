name: http
author: fetchai
version: 0.1.0
license: Apache-2.0
fingerprint: ""
description: "The HTTP connection that wraps a web-based, RESTful API specification."
class_name: HTTPConnection
protocols: ["fetchai/http:0.1.0"]
restricted_to_protocols: ["fetchai/http:0.1.0"]
excluded_protocols: []
config:
  host: ${addr:127.0.0.1}
  port: ${port:8000}
  api_spec_path: ''  # put here the dotted path to your API spec YAML path.
dependencies: {}
