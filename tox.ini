[tox]
envlist = py37
skipsdist = True

[testenv]
basepython = python3.7
whitelist_externals = pipenv
deps =
    pytest
    pytest-cov

commands =
    pipenv install
    pytest --doctest-modules aea tests/ --cov-report=html --cov-report=term --cov=aea {posargs}

[testenv:flake8]
basepython = python3.7
deps = flake8
       flake8-docstrings
       pydocstyle==3.0.0
commands = flake8 aea --exclude=.md,aea/*_pb2.py --ignore=E501,E701